<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testproject">
    <!-- To auto-complete the recepyName text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="RecepyStorer"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditProductActivity"></activity>
        <activity android:name=".RemovingProductActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".RecepyInfoActivity"
            android:parentActivityName=".RecepiesActivity"/>
        <activity android:name=".AddProductsToRecepyActivity"
            android:parentActivityName=".AddRecepyActivity"/>
        <activity
            android:name=".EditRecepyActivity"
            android:parentActivityName=".RecepiesActivity" />
        <activity
            android:name=".AddRecepyActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".RecepiesActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>